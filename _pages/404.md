---
permalink: /404.html
title: "command not found · Oliver Scheiwiller"
description: This route doesn't exist. Don't panic.
term_title: "guest@oschei.com — error — zsh"
sitemap: false
---

{% include prompt.html cmd="<span style='color:var(--text)' id='attempted-path'>cd /everything-i-want</span>" %}

<div class="err-line">zsh: no such file or directory: <span id="attempted-path-echo">/everything-i-want</span></div>

<div class="dont-panic">Don't Panic.</div>

{% include history.html %}

<div class="hint-line"># tip: try <a class="inline-cmd" href="/about/" data-cmd="cat about.md"><code>cat about.md</code></a> or tab to a link above. {% include carrot.html page="404" %}</div>

<script>
  // Reflect the URL the user tried into the error line.
  (function () {
    var p = window.location.pathname || '/';
    var attempted = document.getElementById('attempted-path');
    var echo = document.getElementById('attempted-path-echo');
    if (attempted) attempted.textContent = 'cd ' + p;
    if (echo) echo.textContent = p;
  })();
</script>
